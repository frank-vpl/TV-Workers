 # 📺 TV Workers
 
 پروکسی IPTV مبتنی بر Cloudflare Workers برای استریم کانال‌های HLS با بازنویسی خودکار پلی‌لیست و پشتیبانی از CORS.

[English Version](README.md)

### آموزش ویدئویی:
 
 <video controls width="640">
   <source src="https://raw.githubusercontent.com/frank-vpl/TV-Workers/refs/heads/master/m3u8.mp4" type="video/mp4">
   مرورگر شما از ویدئوی HTML5 پشتیبانی نمی‌کند.
 </video>

 ## 🚀 قابلیت‌ها

 - پروکسی کردن استریم‌های IPTV HLS (`.m3u8`)
 - بازنویسی خودکار URLهای داخل پلی‌لیست
 - پشتیبانی از استریم سگمنت‌ها (`.ts`، `.m4s` و غیره)
 - هدرهای پویا (User-Agent، Referer، Origin)
 - فعال بودن CORS (`Access-Control-Allow-Origin: *`)
 - سبک و سریع (بر پایه Cloudflare Workers)

 ---

 ## 📂 ساختار پروژه

 ```
 worker.js
 README.md
 ```

 ---

 ## 🛠 نحوه کار

 این Worker به شکل زیر کار می‌کند:

 1. شناسهٔ کانال (ID) را از مسیر URL می‌خواند
 2. آن را با شیء `CHANNELS` مطابقت می‌دهد
 3. درخواست را به منبع واقعی IPTV پروکسی می‌کند
 4. پلی‌لیست‌های `.m3u8` را بازنویسی می‌کند تا ترافیک دوباره از طریق Worker شما عبور کند
 5. سگمنت‌های ویدیو را به‌صورت مستقیم استریم می‌کند

 ---

 ## ➕ افزودن یک کانال IPTV

 فایل `worker.js` را باز کرده و بخش زیر را پیدا کنید:

 ```js
 // IPTV Channels
 const CHANNELS = {
   "2342": "https://live.livetvstream.co.uk/LS-63503-4",
   // "1001": "https://example.com/live/stream1"
 }
 ```

 برای افزودن یک استریم IPTV جدید:

 1. آدرس پایهٔ استریم را پیدا کنید  
    مثال:

    ```
    https://live.livetvstream.co.uk/LS-63503-4/index.m3u8
    ```

 2. بخش `/index.m3u8` را حذف کنید  
    فقط مسیر پایه را نگه دارید:

    ```
    https://live.livetvstream.co.uk/LS-63503-4
    ```

 3. آن را به `CHANNELS` اضافه کنید:

 ```js
 const CHANNELS = {
   "2342": "https://live.livetvstream.co.uk/LS-63503-4",
   "1001": "https://example.com/live/channel"
 }
 ```

 ---

 ## 🔗 دسترسی به یک کانال

 بعد از دیپلوی کردن Worker، می‌توانید به استریم‌ها به این شکل دسترسی داشته باشید:

 ```
 https://workername.username.workers.dev/{id}/index.m3u8
 ```

 ### مثال

 اگر آدرس Worker شما این باشد:

 ```
 https://tv-proxy.frank.workers.dev
 ```

 و شناسهٔ کانال شما این باشد:

 ```
 2342
 ```

 آدرس استریم:

 ```
 https://tv-proxy.frank.workers.dev/2342/index.m3u8
 ```

 ---

 ## ☁️ دیپلوی روی Cloudflare Workers

 ### 1️⃣ نصب Wrangler

 ```bash
 npm install -g wrangler
 ```

 ### 2️⃣ ورود (Login)

 ```bash
 wrangler login
 ```

 ### 3️⃣ دیپلوی

 ```bash
 wrangler deploy
 ```

 بعد از دیپلوی، Cloudflare به شما یک آدرس شبیه زیر می‌دهد:

 ```
 https://your-worker-name.your-username.workers.dev
 ```

 ---

 ## 📡 نمونه تنظیمات یک کانال

 منبع (Source):

 ```
 https://live.livetvstream.co.uk/LS-63503-4/index.m3u8
 ```

 تنظیم در Worker:

 ```js
 "2342": "https://live.livetvstream.co.uk/LS-63503-4",
 ```

 آدرس استریم از طریق Worker:

 ```
 https://workername.username.workers.dev/2342/index.m3u8
 ```

 ---

 ## ⚠️ نکات

 - فقط از استریم‌هایی استفاده کنید که اجازهٔ پروکسی کردن آن‌ها را دارید.
 - برخی ارائه‌دهندگان IPTV ممکن است استفاده از پروکسی را مسدود کنند.
 - در صورت نیاز تنظیمات کش (cache) را تغییر دهید.
 - مطمئن شوید که URL پایه شامل `index.m3u8` نباشد.

 ---

 ## 📜 مجوز

 این پروژه تحت مجوز [MIT License](./LICENSE) منتشر شده است.

